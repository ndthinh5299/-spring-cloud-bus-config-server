server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ndthinh5299/-spring-cloud-bus-config-server.git
          search-paths: config-properties
    bus:
      enabled: true
      id: config-server
